---
hide:
  - toc
---

# INDICATOR CATEGORIES

| Outcome | Name | Description | Activity type | Outcome type | Driving force | Source |
|---------|------|-------------|---------------|--------------|---------------|--------|
{% for record in categories -%}
  {%- set id = record['id'] -%}
  {%- set name = record['name'] -%}
  {%- set description = record['description'] -%}
  {%- set dimension = enum_dict['SustainabilityDimension'][record['dimension']]['title'] -%}
  {%- set driving_force = enum_dict['FoodSystemDrivingForce'][record['driving_force']]['title'] if record['driving_force'] else '-' -%}
  {%- set activity_type = enum_dict['FoodSystemActivityType'][record['activity_type']]['title'] if record['activity_type'] else '-' -%}
  {%- set outcome_type = enum_dict['FoodSystemOutcomeType'][record['outcome_type']]['title'] if record['outcome_type'] else '-' -%}
  {%- set sources = record['source'] if record['source'] else [] -%}  
| [{{ dimension }}](../model/SustainabilityDimension) | {{ name }} | {{ description | replace("\n", "") }} | [{{ activity_type }}](../model/FoodSystemActivityType) | [{{ outcome_type }}](../model/FoodSystemOutcomeType) |  [{{ driving_force }}](../model/FoodSystemDrivingForce) |
{%- for source in sources %} [{{ sources_dict[source]['name'] }}](../references) {% endfor -%} 
|
{% endfor %}
