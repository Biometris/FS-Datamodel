# {{ schema.title | upper }}

{{ schema.description }}

## Model diagram

```{{ gen.mermaid_directive() }}
{{ gen.mermaid_diagram() }}
```

## Glossary

| Label | Name | Description | Type |
{% for c in schemaview.all_classes(ordered_by="lexical").values()|sort(attribute='name') -%}
{%- if 'association' not in schemaview.class_ancestors(c.name) and not schemaview.is_mixin(c.name) -%}
| --- | --- | --- | --- |
| {{gen.link(c)}} | [{{ c.name | capitalize }}]({{ gen.uri(c) }}) | {{c.description}} | Class |
{%- endif -%}
{% endfor %}
{% for e in schemaview.all_enums().values()|sort(attribute='name') -%}
| {{gen.link(e)}} | [{{ e.name | capitalize }}]({{ gen.uri(e) }}) |{{e.description}} | Enum |
{% endfor %}
 